#include <stdio.h>
int main() {
    int x = 10;
    int y = 0;
    int z = 0;
    z = x + y;
    if (z > 0) {
        printf("z is positive\n");
    } else {
        printf("z is non-positive\n");
    }
    printf("x = %d, y = %d\n", x, y);

    return 0;
}
